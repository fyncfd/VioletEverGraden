#include <bits/stdc++.h>

int n, x, y, d, t;
int u, v;

void solve() {
	std::cin >> n >> x >> y >> d >> t;
}

signed main() {
	std::ios::sync_with_stdio(false);
	std::cout.tie(nullptr);
	std::cin.tie(nullptr);

	int _ = 1;
	std::cin >> _;

	while (_ --) {
		solve();
	}

	return 0;
}