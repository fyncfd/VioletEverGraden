#include<bits/stdc++.h> 

using namespace std;

int main( )
{
    int n, m
    std::cin >> n;

    std::vector<int> v(n + 1);
    for (int i = 1; i <= n; i ++) {
        std::cin >> v[i];
    }

    int __ = m;
    while (__ --) {
        int t, z;
        std::cin >> t >> z;
        
    }
}